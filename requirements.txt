# Notion → IA → Anki sync
# Instalar: pip install -r requirements.txt

# Core
notion-client==3.0.0
requests==2.33.1
python-dotenv==1.2.2

# Interface visual
streamlit>=1.35.0
Pillow>=10.0.0
customtkinter>=5.2.0
pywebview>=5.0.0

# IA — Claude (AI_PROVIDER=claude)
anthropic==0.99.0

# IA — Gemini (AI_PROVIDER=gemini)
google-genai==1.75.0
