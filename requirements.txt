# Notion → IA → Anki sync
# Instalar: pip install -r requirements.txt

# Core
notion-client==3.0.0
requests==2.33.1
python-dotenv==1.2.2

# Interface visual
flet>=0.24.0
Pillow>=10.0.0
customtkinter>=5.2.0

# IA — Claude (AI_PROVIDER=claude)
anthropic==0.99.0

# IA — Gemini (AI_PROVIDER=gemini)
google-genai==1.75.0

# IA — OpenAI / ChatGPT (AI_PROVIDER=openai)
openai>=1.50.0

# IA — Groq (AI_PROVIDER=groq) — free tier com Llama/Mixtral
groq>=0.11.0
